name: MEEPer
author: moreeyesplz
description: 'Receives MEEP requests and creates an issue'
runs:
  using: 'node12'
  main: 'index.js'
inputs:
  bot_id:
    description: 'App id'
    required: true
  bot_install_id:
    description: 'App install id'
    required: true
  bot_key:
    description: 'Bot pkey'
    required: true
branding:
  icon: external-link
  color: green